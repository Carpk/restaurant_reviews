<div class="feature-box">
  <div class="table-responsive">
    <table class="table">
      <thead>
        <tr>
          <th>#</th>
          <th>Name</th>
          <th>Category</th>
          <th>Score</th>
          <th>Reviews</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
      <% @restaurants.each do |place| %>
        <tr>
          <td><%=place.id%></td>
          <td><%=place.name%></td>
          <td>
            <% place.categories.each do |category| %>
              <%= category.set %>
            <% end %>
          </td>
          <td><%=place.score%></td>
          <td>
            <%= link_to place.reviews.count, admin_restaurant_reviews_path(1, place.name) %>
          </td>
          <td>
            <%= link_to "Delete", admin_restaurant_path(1, place), method: :delete, data: { confirm: 'Are you certain you want to delete this?' } %>-
            <%= link_to "Edit", edit_admin_restaurant_path(1, place.name) %>-
            <%= link_to "Unlist", admin_restaurant_path(1, place) %>-
            <%= link_to "Review", new_admin_restaurant_review_path(1, place) %>
          </td>
        </tr>
        <%end%>
      </tbody>
    </table>
    <%= link_to "Add Restaurant", new_admin_restaurant_path(1) %>
    <%= link_to "Create Category", admin_categories_path(1) %>
  </div>
</div>
